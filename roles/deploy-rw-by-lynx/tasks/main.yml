---
- ansible.builtin.import_tasks: preflight.yml
  tags: [always]

- ansible.builtin.import_tasks: install_docker.yml
  tags: [docker]

- ansible.builtin.import_tasks: sync_repo.yml
  tags: [repo]

- ansible.builtin.import_tasks: collect_pp_flags.yml
  tags: [nginx, config, dns]

- ansible.builtin.import_tasks: derive_config.yml
  tags: [nginx, config, dns]

- ansible.builtin.import_tasks: set_sysctl.yml
  tags: [sysctl]

- ansible.builtin.import_tasks: build_nginx_conf.yml
  tags: [nginx, config]

- ansible.builtin.import_tasks: setup_cloudflare.yml
  tags: [dns]

- ansible.builtin.import_tasks: make_certs.yml
  tags: [certs]

- ansible.builtin.import_tasks: block_asn.yml
  tags: [security, firewall]

- ansible.builtin.import_tasks: run_nginx.yml
  tags: [nginx]

- ansible.builtin.import_tasks: install_remnanode.yml
  tags: [remnanode]
