---
# Kept as a compatibility task file for callers that import it directly.
- name: Assert proxy-protocol map values
  ansible.builtin.assert:
    that: item | int in [0, 1]
    fail_msg: pp_map accepts only 0 or 1 values.
  loop: "{{ pp_map | default([]) }}"
