---
# This file is deliberately non-destructive. CI validates the real deployment
# playbook with --syntax-check; integration tests run in disposable containers.
- name: Validate role test fixture
  hosts: localhost
  gather_facts: false
  connection: local
  tasks:
    - name: Assert test fixture is non-destructive
      ansible.builtin.assert:
        that: true
